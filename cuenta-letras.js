/**
 * Regresa un objeto con el número de veces que cada letra en @texto aparece.
 * @param {string} texto - El texto del que se contarán las letras
 */
function cuentaLetras(texto) {

}

module.exports = cuentaLetras;